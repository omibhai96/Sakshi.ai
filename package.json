{
  "name": "sakshi-ai",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "vite preview --port 10000 --host"  // ✅ Add this for Render
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "axios": "^1.6.0",
    "framer-motion": "^10.0.0",
    "vite": "^4.0.0"  // ✅ Move vite from devDependencies
  },
  "devDependencies": {
    "tailwindcss": "^3.0.0"
  }
}
